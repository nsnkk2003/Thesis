Hi Erik,

I've pushed the updated code. Two improvements:

1. Checkpoints now automatically upload to MinIO after each 
   save — no more manual copying from the container.

2. On startup, the code checks MinIO for the latest checkpoint 
   and automatically resumes from it.

I've already placed the epoch-1 checkpoint you shared into 
MinIO at trained_adapters/checkpoints/checkpoint-8749/.

So whenever you're free to restart the training, just run the 
same docker command as before. The code will automatically 
pick up from where we stopped and continue to epochs 2 and 3. 
No manual steps on your side.

Thank you so much for everything!
