python -c "from minio import Minio; c = Minio('minio.dev01.k8s.aeroes.internal', access_key='YOUR_KEY', secret_key='YOUR_SECRET', secure=True); print([b.name for b in c.list_buckets()])"


Hi Erik,

Quick questions about the K8s cluster for the training job:

1. What namespace should I deploy to?
2. What GPU type is available on the cluster (A100, V100, T4, etc.) 
   and how much VRAM does it have?
3. What is the GitLab container registry URL? 
   (something like registry.gitlab.your-company.com/your-project)

Thanks!
