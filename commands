Hi Erik,

Could you please make two small changes and rebuild?

---

1. In data_loader.py, change this section in __getitem__:

        # Step 1: Download image from MinIO
        image = load_image_from_minio(self.minio_client, self.bucket, item["image_path"])

        # Step 2: Apply augmentation if this is a defect AND augmentation is enabled
        if self.augment and item["label"] == 1:
            image = self.defect_transform(image)

To:

        # Step 1: Download image from MinIO
        image = load_image_from_minio(self.minio_client, self.bucket, item["image_path"])

        # Step 1.5: Resize to 336x336 to prevent AnyRes multi-patch splitting
        # This reduces image tokens from ~2880 to ~576, making training 3-4x faster
        # Melt pool defect patterns are visible at this resolution
        image = image.resize((336, 336))

        # Step 2: Apply augmentation if this is a defect AND augmentation is enabled
        if self.augment and item["label"] == 1:
            image = self.defect_transform(image)

---

2. In config.yaml and config.template.yaml, change:

        max_length: 2800

To:

        max_length: 1024

---

These two changes should bring training time down from ~18 days to around 4-5 days.

Thank you,
Kaushik


Hi Erik,

Thanks for the feedback on the token issue. I've added a test 
script that checks different approaches with random data — 
no need to run the full training.

It tests:
1. How many tokens different image sizes produce
2. Training speed at different settings (10 steps only, ~5 min)
3. Estimates full training time for each setting

Could you run it when you get a chance?

python src/test_speed.py

This will help us find the fastest safe settings before 
committing to another full run. Thank you!



Hi Erik,

I've pushed an optimization — images are now resized to 
336x336 before processing, which drops token count from 
3117 to 1365. max_length is now 1536.

I also fixed the test script. Could you please run the 
test first to verify the speedup?

python src/test_speed.py

If it looks good, we can stop the current run and restart 
with the new settings. Should be ~2-3x faster.

Thank you!
