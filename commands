python -c "from minio import Minio; c = Minio('minio.dev01.k8s.aeroes.internal', access_key='YOUR_KEY', secret_key='YOUR_SECRET', secure=True); print([b.name for b in c.list_buckets()])"

Hi Erik,

Apologies for the delayed response — I’ve been occupied with a move to Gothenburg.

Thank you very much for getting it running. I noticed there are some data matching issues (only a few rows matched with images), so I’ll review and fix this on my end and update the code in GitLab.

May I kindly request you to run it again once I push the update?

Also, thank you for resolving the CUDA 12.4 compatibility issue — I really appreciate it.
